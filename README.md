# Bash-Script-for-Install-Docker
Run this script to install Docker.
Before run it on server linux, maksure to make this script executeable.
Run ```sudo chmod 777 install-docker.sh``` to make it executable.
Run ```sudo ./install-docker.sh``` to run the script
